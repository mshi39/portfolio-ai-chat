import React from "react";import ReactDOM from "react-dom/client";import { ChatWidget } from "./components/chat/ChatWidget";import "./styles/globals.css";ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><main className="min-h-screen bg-canvas"><div className="mx-auto max-w-5xl px-6 py-20"><p className="text-sm font-medium uppercase tracking-[.18em] text-muted">Melissa Shi · Portfolio</p><h1 className="mt-4 max-w-2xl text-4xl font-semibold tracking-tight text-ink sm:text-6xl">Thoughtful design for complex, human problems.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted">This canvas represents the existing Webflow portfolio. Open the chat in the lower-right corner to preview the widget.</p></div><ChatWidget/></main></React.StrictMode>);