import type { KeyboardEvent } from "react";
import { SendIcon } from "../icons/Icons";
interface Props { value: string; onChange: (value: string) => void; onSubmit: () => void; isDisabled?: boolean; }
export function ChatComposer({ value, onChange, onSubmit, isDisabled = false }: Props) { const canSend = value.trim().length > 0 && !isDisabled; function handleKeyDown(event: KeyboardEvent<HTMLTextAreaElement>) { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); if (canSend) onSubmit(); } } return <div className="border-t border-brand/10 bg-white p-3"><div className="flex items-end gap-2 rounded-2xl border-2 border-brand/15 bg-white p-2 pl-3 transition focus-within:border-brand focus-within:ring-4 focus-within:ring-brand/10"><label htmlFor="chat-message" className="sr-only">Ask Melissa&apos;s AI a question</label><textarea id="chat-message" rows={1} value={value} disabled={isDisabled} placeholder="Ask me anything…" onChange={(event) => onChange(event.target.value)} onKeyDown={handleKeyDown} className="max-h-28 min-h-10 flex-1 resize-none bg-transparent py-2 text-sm leading-5 text-ink outline-none placeholder:text-slate/60" /><button type="button" disabled={!canSend} onClick={onSubmit} aria-label="Send message" className="flex size-10 shrink-0 items-center justify-center rounded-full bg-brand text-white transition hover:-translate-y-0.5 hover:bg-brand-dark disabled:cursor-not-allowed disabled:bg-brand/25"><SendIcon className="size-5" /></button></div><p className="mt-2 text-center text-[10px] leading-4 text-slate/60">AI can make mistakes. Check Melissa&apos;s original work for details.</p></div>; }
