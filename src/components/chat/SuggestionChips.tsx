import { SUGGESTED_QUESTIONS } from "../../config/chatContent";
export function SuggestionChips({ onSelect }: { onSelect: (question: string) => void }) { return <div className="ml-11 mt-5 flex flex-wrap gap-2" aria-label="Suggested questions">{SUGGESTED_QUESTIONS.map(({ icon, label }) => <button key={label} type="button" onClick={() => onSelect(label)} className="rounded-full border-2 border-brand/20 bg-white px-3.5 py-2 text-left text-xs font-bold text-brand transition hover:-translate-y-0.5 hover:border-brand hover:bg-brand-soft hover:shadow-sm active:translate-y-0"><span className="mr-1.5" aria-hidden="true">{icon}</span>{label}</button>)}</div>; }
