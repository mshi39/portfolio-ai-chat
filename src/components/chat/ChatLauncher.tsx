import melissaHeadshot from "../../assets/melissa-headshot.png";
import { CloseIcon } from "../icons/Icons";
interface Props { isOpen: boolean; onToggle: () => void; }
export function ChatLauncher({ isOpen, onToggle }: Props) { return <button type="button" aria-label={isOpen ? "Close chat" : "Open chat with Melissa's AI"} aria-expanded={isOpen} aria-controls="melissa-chat-panel" onClick={onToggle} className="group relative flex size-16 items-center justify-center overflow-hidden rounded-full border-[3px] border-white bg-brand text-white shadow-launcher transition duration-200 hover:-translate-y-1 hover:shadow-launcher-hover active:translate-y-0"><img src={melissaHeadshot} alt="" className={"absolute inset-0 size-full object-cover transition duration-200 " + (isOpen ? "scale-110 opacity-0" : "scale-100 opacity-100")} /><span className={"absolute inset-0 bg-brand transition duration-200 " + (isOpen ? "opacity-100" : "opacity-0")} /><CloseIcon className={"relative size-7 transition duration-200 " + (isOpen ? "scale-100 opacity-100" : "scale-75 opacity-0")} /></button>; }
